# =====================================================================
# PERFIL: dev
# Uso recomendado: desarrollo local y pruebas manuales.
# =====================================================================
spring:
  datasource:
    # Dev usa PostgreSQL (permite defaults para facilitar arranque local).
    url: ${DB_URL:jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:6543/postgres}
    username: ${DB_USERNAME:postgres.wczdgaujomnojlszzvhy}
    password: ${DB_PASSWORD:Kodi_remr11}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 20000

  jpa:
    # En dev permitimos update para iterar rapido con entidades.
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
